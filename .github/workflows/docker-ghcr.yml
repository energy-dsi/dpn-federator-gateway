# SPDX-License-Identifier: Apache-2.0
# © Crown Copyright 2025. This work has been developed by the National Digital Twin Programme
# and is legally attributed to the Department for Business and Trade (UK) as the governing entity.

# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Build and release docker images to GHCR

on:
  workflow_call:
    inputs:
      image_tag:
        required: true
        description: "The tag(s) to apply to the docker images, if multiple use a comma separated list. Don't use staged as a tag."
        type: string
      jar_version:
        required: true
        description: "The version of the jar to use for the docker image"
        type: string
      docker_target:
        required: true
        description: "The target of the multistage docker build to use"
        type: string
      dry_run:
        required: false
        description: "Dry Run. Whether to push the images to GHCR or not"
        type: boolean

jobs:
  release:
    name: Build and release docker images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Validate docker target
        env:
          DOCKER_TARGET: ${{ inputs.docker_target }}
        run: |
          if [[ "${DOCKER_TARGET}" != "federator" && "${DOCKER_TARGET}" != "federator-msk" ]]; then
            echo "Docker target is invalid. Use either federator or federator-msk"
            exit 1
          fi

      - name: Checkout repo
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Login to ghcr.io
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Format repo name
        run: echo "REPO=${GITHUB_REPOSITORY,,}" >>${GITHUB_ENV}

      - name: Get server and client jars
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5.0.0
        with:
          pattern: federator-*.jar
          path: target
          merge-multiple: true

      - name: Build Server Image
        env:
          JAR_VERSION: ${{ inputs.jar_version }}
          DOCKER_TARGET: ${{ inputs.docker_target }}
          WORKSPACE: ${{ github.workspace }}
        run: docker build --no-cache --build-arg JAR_NAME="federator-server-${JAR_VERSION}" -t ghcr.io/${REPO}/federator-server:staged -f "$WORKSPACE/docker/Dockerfile.server" --target ${DOCKER_TARGET} .

      - name: Run Trivy Scan on Server Docker Image
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0
        with:
          image-ref: "ghcr.io/${{ env.REPO }}/federator-server:staged"
          format: "table"
          exit-code: "1"
          ignore-unfixed: true
          severity: "CRITICAL,HIGH"
        continue-on-error: true

      - name: Tag Server Image with tag(s) ${{ inputs.image_tag }}
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          ./.github/actions/docker-tags.sh "ghcr.io/${REPO}/federator-server" "${IMAGE_TAG}"
          docker rmi ghcr.io/${REPO}/federator-server:staged

      - name: Build Client Image
        env:
          JAR_VERSION: ${{ inputs.jar_version }}
          DOCKER_TARGET: ${{ inputs.docker_target }}
          WORKSPACE: ${{ github.workspace }}
        run: docker build --no-cache --build-arg JAR_NAME="federator-client-${JAR_VERSION}" -t ghcr.io/${REPO}/federator-client:staged -f "$WORKSPACE/docker/Dockerfile.client" --target ${DOCKER_TARGET} .

      - name: Run Trivy Scan on Client Docker Image
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0
        with:
          image-ref: "ghcr.io/${{ env.REPO }}/federator-client:staged"
          format: "table"
          exit-code: "1"
          ignore-unfixed: true
          severity: "CRITICAL,HIGH"
        continue-on-error: true

      - name: Tag Client Image with tag(s) ${{ inputs.image_tag }}
        env:
          IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          ./.github/actions/docker-tags.sh "ghcr.io/${REPO}/federator-client" "${IMAGE_TAG}"
          docker rmi ghcr.io/${REPO}/federator-client:staged

      # PUSH DISABLED: GHCR is populated by the central 'push-all-images-to-ghcr'
      # pipeline. Build + Trivy scan are retained; pushes are turned off.
      - name: Push Server Image
        if: false   # was: ${{ !inputs.dry_run }}
        run: echo "GHCR push disabled - central pipeline publishes images. (skipped docker push --all-tags ghcr.io/${REPO}/federator-server)"

      - name: Push Client Image
        if: false   # was: ${{ !inputs.dry_run }}
        run: echo "GHCR push disabled - central pipeline publishes images. (skipped docker push --all-tags ghcr.io/${REPO}/federator-client)"

