booleanValue=false
stringValue=string
intValue=10
longValue=20
durationValue=PT1S
testValue=notOverridden
scheduler.start_delay=1
scheduler.poll_period=1
file.location=test.properties
kafka.bootstrapServers=test
kafka.consumerGroup=test
kafka.pollRecords=PT10S

# Test Configuration for Unit Tests
# Management Node Settings
management.node.base.url=https://localhost:8090
management.node.request.timeout=30

# Keycloak Settings (Test values - not used in unit tests)
keycloak.base.url=https://localhost:8443
keycloak.realm=test-realm
keycloak.client.id=TEST_CLIENT
keycloak.client.secret=test-secret
keycloak.token.endpoint=/realms/test-realm/protocol/openid-connect/token

# SSL Configuration (Test paths - these files don't need to exist for unit tests)
# The paths just need to be valid strings for the properties to load
ssl.keystore.path=test-keystore.jks
ssl.keystore.password=testpass
ssl.truststore.path=test-truststore.jks
ssl.truststore.password=testpass

# Cache Configuration
cache.ttl.seconds=3600